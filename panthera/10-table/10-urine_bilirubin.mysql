CREATE TABLE `urine_billirubin` (
    `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
    `user` INT UNSIGNED NOT NULL,
    `stamp` BIGINT UNSIGNED NOT NULL,
    `billirubin` TINYINT UNSIGNED NOT NULL,
    KEY `index` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
