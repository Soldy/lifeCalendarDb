CREATE TABLE `urine_nitrite` (
    `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
    `user` INT UNSIGNED NOT NULL,
    `stamp` BIGINT UNSIGNED NOT NULL,
    `nitrite` ENUM('negative', 'trace', 'positive'),
    KEY `index` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
