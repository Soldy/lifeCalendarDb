CREATE TABLE `urine_ph` (
    `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
    `user` INT UNSIGNED NOT NULL,
    `stamp` BIGINT UNSIGNED NOT NULL,
    `ph` TINYINT UNSIGNED NOT NULL,
    KEY `index` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
